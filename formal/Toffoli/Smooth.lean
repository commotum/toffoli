import Toffoli.Smooth.CircleGate

/-!
# Smooth extensions

This thin facade exposes the corrected circle-valued smooth extension.  It is intentionally not
imported by the root discrete facade: manifold dependencies remain isolated until the main smooth
extension theorem is integrated.
-/
